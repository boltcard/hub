# environment config (contains domain)
.env
# test builds
docker/card/card
notes
.github/prompts/*
# local planning docs (brainstorm specs + implementation plans — kept local, not committed)
docs/plans/
docs/superpowers/
