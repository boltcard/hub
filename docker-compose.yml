services:
  phoenix:
    container_name: phoenix
    image: acinq/phoenixd:0.8.0
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 384M
    # phoenix-cli reads the http-password from /phoenix/.phoenix/phoenix.conf and
    # calls the local HTTP API — a success means phoenixd is up and answering.
    # Lets `card` wait for service_healthy instead of racing the port at boot.
    healthcheck:
      test: ["CMD", "/phoenix/phoenix-cli", "getinfo"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    volumes:
      - phoenix_data:/phoenix/.phoenix
    networks:
      - hubnet
  card:
    image: boltcard/card:latest
    container_name: card
    restart: unless-stopped
    build: ./docker/card
    env_file: .env
    environment:
      - GOMEMLIMIT=150MiB
    develop:
      watch:
        - path: ./docker/card
          action: rebuild
    deploy:
      resources:
        limits:
          memory: 192M
    depends_on:
      phoenix:
        condition: service_healthy
      webproxy:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-fsS", "--head", "http://localhost:8000/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    volumes:
      - type: volume
        source: phoenix_data
        target: /root/.phoenix
        read_only: true
      - type: volume
        source: card_data
        target: /card_data
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - hubnet
  webproxy:
    image: boltcard/webproxy:latest
    container_name: webproxy
    build: ./docker/webproxy
    restart: unless-stopped
    env_file: .env
    deploy:
      resources:
        limits:
          memory: 192M
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    # sysctls for QUIC/HTTP3 (uncomment if supported by host kernel):
    # sysctls:
    #   - net.core.rmem_max=7500000
    #   - net.core.wmem_max=7500000
    networks:
      - hubnet
networks:
  hubnet:
volumes:
  phoenix_data:
  caddy_data:
  caddy_config:
  card_data:
